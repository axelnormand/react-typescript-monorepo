
{
  // prevent an incorrect invocation of tsc without -b
  "files": [], 
  // listing all projects here allows tsc -b to work
  "references": [{ "path": "common" }, { "path": "app1" }, { "path": "app2" }]
}
